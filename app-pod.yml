apiVersion: v1
kind: Pod
metadata:
  name: app-1
  labels:
    app: app-1
spec:
  containers:
  - name: app-1-container
    image: docker.io/npaduladvr/k8s-learning:1.0
    ports:
    - name: express-port
      containerPort: 3000
